{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Nautobot Cellular SoT App Configuration",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "operational_snapshot_ttl_seconds": {
      "type": "integer",
      "minimum": 60,
      "maximum": 86400,
      "default": 900,
      "description": "Maximum expected age for latest normalized operational snapshots before they should be treated as stale."
    },
    "sync_batch_size": {
      "type": "integer",
      "minimum": 1,
      "maximum": 10000,
      "default": 500,
      "description": "Maximum number of cellular records processed per bounded synchronization batch."
    },
    "prometheus_export_enabled": {
      "type": "boolean",
      "default": true,
      "description": "Enable the bounded-cardinality Prometheus text export endpoint."
    }
  }
}
